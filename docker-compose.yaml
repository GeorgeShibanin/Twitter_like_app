version: "3"

services:
  app:
    build: .
    ports:
      - 8080
    environment:
      MONGO_URL: 'mongodb://database:27017'
      MONGO_DBNAME: 'microblogAPI'
      SERVER_PORT: 8080
      STORAGE_MODE: 'memory'
  database:
    image: mongo:latest
    ports:
      - 27017